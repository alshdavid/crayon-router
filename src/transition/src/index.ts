export * from './loader.js'

import { 
    loader,
    slideUp,
    slideDown,
    slideLeft,
    slideRight,
    pushUp,
    pushDown,
    pushLeft,
    pushRight,
    popUp,
    popDown,
    popLeft,
    popRight,
    fade
} from './loader.js'

export default { 
    loader,
    slideUp,
    slideDown,
    slideLeft,
    slideRight,
    pushUp,
    pushDown,
    pushLeft,
    pushRight,
    popUp,
    popDown,
    popLeft,
    popRight,
    fade
}