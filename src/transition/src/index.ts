export * from './loader'

import { 
    loader,
    slideUp,
    slideDown,
    slideLeft,
    slideRight,
    pushUp,
    pushDown,
    pushLeft,
    pushRight,
    popUp,
    popDown,
    popLeft,
    popRight,
    fade
} from './loader'

export default { 
    loader,
    slideUp,
    slideDown,
    slideLeft,
    slideRight,
    pushUp,
    pushDown,
    pushLeft,
    pushRight,
    popUp,
    popDown,
    popLeft,
    popRight,
    fade
}