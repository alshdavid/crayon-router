import { handlerFunc } from "../router/types";
import { AnimationRoute } from "./types";

export const route = (
    routes:  AnimationRoute[]
): handlerFunc => (
    req, res, state, app
) => {
    
}