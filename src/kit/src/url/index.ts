import { 
  normalise,
  removeTrailingSlash,
  hasTrailingSlash,
  matchPath,
  deserializeQuery,
} from './url'

export const url = {
  normalise,
  removeTrailingSlash,
  hasTrailingSlash,
  matchPath,
  deserializeQuery,
}

export default url
